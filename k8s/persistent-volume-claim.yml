apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: internship
  name: nadah-internship-pvc
  labels:
    type: local
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 0.1Gi
  volumeName: nadah-internship-pv
  storageClassName: standard
